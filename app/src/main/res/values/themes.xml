<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CobainUI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CobainUI" parent="Base.Theme.CobainUI" />
    <!-- Style Kustom untuk Bottom Navigation -->
    <style name="CustomBottomNavigationView"
        parent="Widget.MaterialComponents.BottomNavigationView">
        <!-- Mengatur ukuran ikon. Coba ubah nilainya (misal: 20dp, 28dp) -->
        <item name="itemIconSize">30dp</item>
        <!-- Mengatur padding di sekitar ikon -->
        <item name="itemIconPadding">4dp</item>
        <item name="android:color">#4D9C27B0</item>
    </style>
</resources>